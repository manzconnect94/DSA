{
  "name": "mern-interview-boilerplate",
  "version": "1.0.0",
  "private": true,
  "description": "MERN Task Manager boilerplate built as interview revision material. Every interview-relevant concept is implemented AND commented.",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client",
    "seed": "npm run seed --prefix server",
    "test": "npm test --prefix server",
    "test:client": "npm test --prefix client",
    "cluster": "npm run cluster --prefix server"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
